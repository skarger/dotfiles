# directories
alias cwc="cd ${WORK_CODE_DIRECTORY}"
alias cfs="cd ${WORK_CODE_DIRECTORY}/content-flow-service"
alias cfe="cd ${WORK_CODE_DIRECTORY}/content-flow-engine"
alias wfs="cd ${WORK_CODE_DIRECTORY}/ui-workflow-support"
alias dand="cd ${WORK_CODE_DIRECTORY}/dandelion"
alias aud="cd ${WORK_CODE_DIRECTORY}/audits-service"

alias cpc="cd ${PERSONAL_CODE_DIRECTORY}"
alias flt="cd ${PERSONAL_CODE_DIRECTORY}/flowterie"


alias be="bundle exec"
alias brs="bundle exec rspec"
alias s="echo 'bundle exec rails server'; bundle exec rails server"
alias c="echo 'bundle exec rails console'; bundle exec rails console"
alias r="echo 'bundle exec rspec spec'; bundle exec rspec spec"

alias fn="find . -name"

alias ag='ag --path-to-ignore ~/.agignore'

# git
alias got="git"
alias gs="git status"
alias gb="git branch"
alias gf="git fetch"
alias gp="git pull"
alias gl="git log"
alias gd="git diff"
alias gg="git grep"
alias gcp="git cherry-pick"
alias gco="git checkout"
alias ga="git add"
alias gscrub="git branch | grep -v master | xargs git branch -D"

alias d="docker"
alias dml="echo 'docker-machine ls'; docker-machine ls"
alias dme="echo 'docker-machine env default'; docker-machine env default"
alias dms="echo 'docker-machine start default'; docker-machine start default"
alias edm='echo "eval \$(docker-machine env default)"; eval $(docker-machine env default)'
alias dps='echo "docker ps"; docker ps'
alias di='echo "docker images"; docker images'
alias dc="docker-compose"
alias dcu='echo "docker-compose up"; docker-compose up'
alias dcdd='echo "docker-compose up web worker"; docker-compose up web worker'
alias dshdd='docker exec -it dynamicdropoff_web_1 /bin/bash'
alias dshddt='echo "docker-compose run --rm tests bash"; docker-compose run --rm tests bash'
alias ecr='echo "aws ecr get-login | bash"; aws ecr get-login | bash'

# vim
alias vim='/usr/local/bin/vim'

# python
alias python='python3'
alias pip='pip3'

# Gemfile
alias ipry="ruby ${PERSONAL_CODE_DIRECTORY}/dotfiles/scripts/install_pry_gem.rb"

# kubernetes
alias cfdeployp="open https://jenkins2.ops.salsify.com/job/content-flow-service-deploy/"
alias cfdeploys="open https://jenkins2.ops.salsify.com/job/staging-content-flow-service-deploy/"

# show linked NPM modules
alias linked="( ls -l node_modules ; ls -l node_modules/* ) | grep ^l"

# Ember
alias edl="yarn run ember dependency-lint"
